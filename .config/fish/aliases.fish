# Fish aliases

# Git aliases
alias gs="git status -sb"
alias gh="git hist"
alias ga="git add"
alias gb="git branch"
alias gc="git commit"
alias gd="git diff"
alias gco="git checkout"
alias grh="git reset --hard"
# alias gpr="git pull --rebase"
alias gundo="git reset --soft HEAD^"
alias gfix="git commit --amend"
alias gcount="git shortlog -sn"
alias gunadd="reset -p"

# Emacs gui frame
# alias e="emacsclient -c -n -a ''"

# Replacement for old commands
alias grep="rg"
alias ls="exa"
alias top="htop"
alias diff="diff -y"

# Newsboat newsreader
alias nb="newsboat -C ~/.config/newsboat/config"

# harsh stuff
alias h="harsh"
alias hc="nvim ~/.config/harsh/habits"
alias hl="nvim ~/.config/harsh/log"

alias pass="gopass"
# alias ls='ls -GFh'



